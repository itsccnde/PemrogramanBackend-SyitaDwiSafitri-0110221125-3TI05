const user = {
    name : "Safitri",
    age : 20,
    address : "Banyumas",
    isMarried : false,
};

const {name, age, address} = user;

console.log(name);
console.log(age);
console.log(address);